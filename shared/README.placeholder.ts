// Cross-FE/BE shared types (CLAUDE.md §6): generated from the backend's
// Pydantic models via FastAPI's OpenAPI schema, not hand-duplicated.
// Empty in this milestone because no domain-facing endpoints exist yet
// (only /healthz) — the generation step (e.g. `openapi-typescript`) is
// wired up once there is a real schema worth generating from.
export {};
