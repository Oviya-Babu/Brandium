# Infrastructure as Code (Technology Stack §29) is deliberately empty in
# this milestone: Phase 0 is local-only (Docker Compose), and cloud
# infrastructure (ECS Fargate, RDS, ElastiCache, S3, IAM, plus the
# GPU-capable compute path the Technology Stack v1.1 amendment's §15/§25
# notes are now required for the self-hosted Ollama service) is Phase 4
# (Platform Engineering) scope. An empty placeholder here — rather than
# partially-written, untested resources — avoids exactly the kind of
# half-finished cross-module stub CLAUDE.md §8 prohibits.
